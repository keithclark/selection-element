{
  "name": "@keithclark/selection-element",
  "version": "1.2.0",
  "description": "A web component for listing and selecting other HTML elements",
  "main": "src/SelectionElement.js",
  "scripts": {
    "build": "rollup -c rollup.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Keith Clark",
  "license": "MIT",
  "devDependencies": {
    "rollup-plugin-terser": "^7.0.2"
  }
}
